services:
  pupil:
    build: .
    ports:
      - "3001:3000"
    volumes:
      - pupil-data:/app/data
    environment:
      - DB_PATH=/app/data/pupil.db
    restart: unless-stopped

volumes:
  pupil-data:
    driver: local
